import { BookOpen, LayoutDashboard, LogOut, ShieldCheck, Users } from 'lucide-react';
import { NavLink, Outlet } from 'react-router-dom';
import { useAuth } from '../auth/AuthContext';
export function AppShell() { const { user, logout } = useAuth(); const isAdmin = user?.roles.includes('Administrator'); const isEducator = user?.roles.includes('Educator') || isAdmin; return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">M</span><span>MedPath <em>AI</em></span></div><nav><NavLink to="/dashboard"><LayoutDashboard size={18}/>Dashboard</NavLink><NavLink to="/courses"><BookOpen size={18}/>Courses</NavLink>{isEducator && <NavLink to="/courses/new"><BookOpen size={18}/>Course builder</NavLink>}{isAdmin && <NavLink to="/users"><Users size={18}/>Users</NavLink>}{isAdmin && <NavLink to="/audit"><ShieldCheck size={18}/>Audit log</NavLink>}</nav><button className="logout" onClick={logout}><LogOut size={18}/>Sign out</button></aside><main className="main"><header className="topbar"><div><span className="eyebrow">Clinical learning workspace</span><h1>Good to see you, {user?.displayName.split(' ')[0]}</h1></div><div className="profile"><span>{user?.displayName}</span><small>{user?.roles.join(' · ')}</small></div></header><div className="content"><Outlet/></div></main></div>; }
