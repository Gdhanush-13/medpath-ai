import { useState } from 'react';
import { Navigate, useNavigate } from 'react-router-dom';
import { useAuth } from '../auth/AuthContext';
import { Button } from '../components/Ui';
export function LoginPage() { const { user, login } = useAuth(); const navigate = useNavigate(); const [email, setEmail] = useState('student@medpath.local'); const [password, setPassword] = useState('MedPath123!Local'); const [error, setError] = useState(''); if (user) return <Navigate to="/dashboard" replace/>; return <div className="auth-page"><div className="auth-panel"><div className="brand"><span className="brand-mark">M</span><span>MedPath <em>AI</em></span></div><span className="eyebrow">Healthcare education platform</span><h1>Learn with clarity. Practice with confidence.</h1><p className="muted">A focused workspace for clinical learning, assessments, and educator-led progress.</p><form onSubmit={async event => { event.preventDefault(); setError(''); try { await login(email, password); navigate('/dashboard'); } catch (e) { setError(e instanceof Error ? e.message : 'Unable to sign in'); } }}><label>Email<input value={email} onChange={e => setEmail(e.target.value)} type="email" required/></label><label>Password<input value={password} onChange={e => setPassword(e.target.value)} type="password" required/></label>{error && <div className="error-state">{error}</div>}<Button type="submit">Sign in</Button></form><small className="muted">Local demo: student@medpath.local · MedPath123!Local</small></div><div className="auth-aside"><span className="eyebrow">Built for thoughtful practice</span><h2>Turn structured learning into measurable progress.</h2><div className="quote">“Small, consistent learning moments compound into confident practice.”</div></div></div>; }
